---
title: 'Verify OTP'
description: 'Verify a one-time password and exchange it for an authenticated session with access and refresh tokens.'
---

Verify the 6-digit OTP code sent to a user's email. On success, returns a user object and authentication tokens.

## Request

**`POST /api/v1/auth/verify-otp`**

No authentication required.

### Body

<ParamField body="email" type="string" required>
  The email address the OTP was sent to.
</ParamField>

<ParamField body="token" type="string" required>
  The 6-digit OTP code received by the user.
</ParamField>

## Response

<ResponseField name="success" type="boolean" required>
  Indicates whether the request succeeded. Always `true` on success.
</ResponseField>

<ResponseField name="data" type="object">
  <Expandable title="properties">
    <ResponseField name="user" type="object">
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Unique user identifier.
        </ResponseField>
        <ResponseField name="email" type="string">
          User's email address.
        </ResponseField>
        <ResponseField name="emailVerified" type="boolean">
          Whether the user's email has been verified.
        </ResponseField>
        <ResponseField name="provider" type="string">
          Authentication provider. One of: `email`, `google`, `apple`.
        </ResponseField>
        <ResponseField name="createdAt" type="string">
          ISO 8601 timestamp of account creation.
        </ResponseField>
        <ResponseField name="updatedAt" type="string">
          ISO 8601 timestamp of the last account update.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tokens" type="object">
      <Expandable title="properties">
        <ResponseField name="accessToken" type="string">
          JWT access token for authenticating subsequent requests.
        </ResponseField>
        <ResponseField name="refreshToken" type="string">
          Token used to obtain a new access token when the current one expires.
        </ResponseField>
        <ResponseField name="expiresIn" type="number">
          Access token lifetime in seconds.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Examples

<CodeGroup>

```bash cURL
curl --request POST \
  --url https://your-api.com/api/v1/auth/verify-otp \
  --header 'Content-Type: application/json' \
  --data '{
    "email": "user@example.com",
    "token": "123456"
  }'
```

```typescript TypeScript
const response = await fetch('https://your-api.com/api/v1/auth/verify-otp', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    email: 'user@example.com',
    token: '123456',
  }),
});

const data = await response.json();
// data.data.user - authenticated user
// data.data.tokens - access and refresh tokens
```

</CodeGroup>

### Success response

```json 200
{
  "success": true,
  "data": {
    "user": {
      "id": "usr_01h9...",
      "email": "user@example.com",
      "emailVerified": true,
      "provider": "email",
      "createdAt": "2024-01-15T10:30:00.000Z",
      "updatedAt": "2024-01-15T10:30:00.000Z"
    },
    "tokens": {
      "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
      "refreshToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
      "expiresIn": 3600
    }
  }
}
```

### Error responses

```json 400
{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Invalid email address"
  }
}
```

```json 401
{
  "success": false,
  "error": {
    "code": "UNAUTHORIZED",
    "message": "Invalid or expired OTP"
  }
}
```
